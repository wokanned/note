# STM32 编程学习笔记

## 目录

- [第0章：认识 MCU（微控制器 / 单片机）](#第0章认识mcu微控制器单片机)
  - [0.1 什么是 MCU](#01-什么是-mcu)
  - [0.2 定位与用途](#02-定位与用途)
  - [0.3 定义需求：控制周期 / 实时预算](#03-定义需求控制周期-实时预算)
  - [0.4 内核梯队粗筛（常用 MCU 内核排名）](#04-内核梯队粗筛常用-mcu-内核排名)
  - [0.5 主频 × Flash 等待周期 × 预取加速器](#05-主频--flash-等待周期--预取加速器)
  - [0.6 专用硬件：FPU / DSP / CORDIC / HRTIM](#06-专用硬件fpu--dsp--cordic--hrtim)
  - [0.7 内存 + 外设 + 封装](#07-内存--外设--封装)
  - [0.8 中断延迟 + 功耗](#08-中断延迟--功耗)
- [第1章：C语言与STM32编程基础](#第1章c语言与stm32编程基础)
  - [1.1 数据定义](#11-数据定义)
  - [1.2 结构体与全局变量管理](#12-结构体与全局变量管理)
  - [1.3 编程规范与位操作](#13-编程规范与位操作)
- [第2章：开发环境与调试](#第2章开发环境与调试)
- [第3章：STM32时钟系统](#第3章stm32时钟系统)
- [第4章：GPIO外设与HAL库配置](#第4章gpio外设与hal库配置)
- [第5章：中断与实时处理](#第5章中断与实时处理)
- [第6章：定时与延时](#第6章定时与延时)
- [第7章：PWM 脉冲宽度调制](#第7章pwm-脉冲宽度调制)
- [第8章：ADC 模数转换](#第8章adc-模数转换)
- [第9章：UART 串口通信](#第9章uart-串口通信)
  - [9.1 USART 和 UART 的区别](#91-usart-和-uart-的区别)
  - [9.2 CubeMX 中 USART 的 Mode 说明](#92-cubemx-中-usart-的-mode-说明)
  - [9.3 补充说明](#93-补充说明)
- [第10章：I2C 通信协议](#第10章i2c-通信协议)
  - [10.1 物理层基础](#101-物理层基础)
  - [10.2 核心时序规则](#102-核心时序规则)
  - [10.3 字节级传输规则](#103-字节级传输规则)
- [第11章：FreeRTOS 实时操作系统](#第11章freertos-实时操作系统)
  - [11.1 消息队列](#111-消息队列)
  - [11.2 信号量](#112-信号量)
  - [11.3 任务延时](#113-任务延时)
  - [11.4 配置注意事项](#114-配置注意事项)
- [第12章：HAL 库函数使用指南](#第12章hal-库函数使用指南)
- [第13章：实用编程技巧](#第13章实用编程技巧)

---

## 第 0 章：认识 MCU（微控制器 / 单片机）

### 0.1 什么是 MCU

**MCU**（**Microcontroller Unit**，微控制器单元）中文通常叫 **单片机** 或 **微控制器**。本笔记的主角 STM32 就是一款典型的 MCU。

MCU 在一颗芯片里集成了完整的计算机硬件：

- **CPU 核心**：取指、译码、执行程序（STM32 使用 ARM Cortex-M 内核）。
- **存储器**：**Flash** 存放程序，掉电不丢；**SRAM** 存放运行时的数据，掉电丢失。
- **基础外设**：GPIO、定时器、ADC、UART、I2C、SPI、PWM 等，CPU 通过寄存器直接操作。

也就是说，MCU **内置·存储和内存，相当于一台完整的微型计算机**——上电后直接从 Flash 取指运行，无需外接内存，也无需加载操作系统。

### 0.2 定位与用途

- **资源受限**：Flash / SRAM 通常以 KB~MB 计，与通用电脑相差几个数量级。
- **优势**：功耗低、成本低、体积小、外设直接访问、时序可预测。
- **典型场景**：实时控制与嵌入式控制——电机控制、传感器采集、家电、IoT、汽车电子等。

为什么 MCU 适合实时控制？关键不在 CPU 快慢，而在**硬件协同**：

- 定时器可精确产生 PWM，并在固定时刻自动触发 ADC / 中断，不依赖 CPU 忙等；
- 中断响应有上界（低抖动），配合裸机 / RTOS 可实现确定性的微秒级控制周期；
- 这些正是后续章节（第 5 章中断、第 6 章定时、第 7 章 PWM、第 11 章 FreeRTOS）的基础。

### 0.3 定义需求：控制周期 / 实时预算

选型时按以下步骤从性能角度筛选，先定"够不够"再比"快不快"：控制周期 / 实时预算（如 20kHz FOC = 50µs 一个电流环）、硬截止时间、ISR 负载、内存 / 功耗预算、成本、电压 / 温度、生态与供货。

### 0.4 内核梯队粗筛

常见内核一眼看：STM32 用 M0/M0+/M3/M4/M7/M33；ESP32 用 Xtensa / RISC-V；TI C2000 用 C28x；RP2350 用 M33 + RISC-V；8051 已属遗留。

Cortex-M 官方梯队（DMIPS/MHz / CoreMark/MHz）：

| 内核 | DMIPS/MHz | CoreMark/MHz | 备注 |
|---|---|---|---|
| M0 | 0.87 | 2.33 | 最简 |
| M0+ | 0.95 | 2.46 | 最省电 |
| M23 | 0.98 | 2.64 | M0+ 安全版 |
| M3 | 1.25 | 3.34 | 无 DSP/FPU |
| M4 | 1.25 | 3.42 | +DSP/FPU |
| M33 | 1.50 | 4.02 | M4 升级版 |
| M55 | 1.60 | 4.2 | Helium 向量 |
| M7 | 2.14 | 5.01 | 双发射 + 缓存/TCM |
| M85 | 3.13 | 6.28 | 家族最强 |

注意：①排序非严格全序，主频 + 内存可反超（M4@168MHz 反超 M7@300MHz）；②M4 vs M3 差距小（同为 1.25，M4 赢在 DSP/FPU）；③8051 是 8 位基线，比 M0 慢 1–2 个数量级。

### 0.5 主频 × Flash 等待周期 × 预取加速器

- 理论算力 = 主频 × CoreMark/MHz（DMIPS 已过时）。
- >30–50MHz 后 Flash 取指插入等待周期，算力不随主频线性；看该主频下等待周期数 + 预取加速器（ST ART）能否兜到零等待。
- 普通 SRAM 不一定比 Flash 快（共享总线争用）；紧耦合 SRAM（CCM/ITCM/DTCM）才是确定性快路径。

### 0.6 专用硬件：FPU / CORDIC / HRTIM

- FPU 单精度（M4F/M7/M33）；记得开 `-mfloat-abi=hard`，否则退回软件浮点慢 18–100 倍。
- Helium（M55/M85）向量加速 4–15 倍，但小控制核收益有限。
- CORDIC（sin/cos）、HRTIM（高分辨率定时器）、FMAC（滤波）——ST G4 等带。
- 对 FOC：决定性的是 **PWM↔ADC 硬件同步链**（互补 PWM + 死区 + Break、定时器自动触发 ADC、编码器接口），链不存在则 MIPS 再高也没用。

### 0.7 内存 + 外设 + 封装

- Flash/SRAM 容量决定应用规模；外设看"质量"：PWM 死区/Break、ADC 硬件同步、编码器接口、比较器直连故障脚、DMA。
- 封装决定引脚 / 外设可用数 / 散热 / 成本；异构多核（M0+ + M4）做低功耗 + 实时分工。

### 0.8 中断延迟 + 功耗

- 标称 IRQ 延迟（cycle）：M0=16、M0+=15、M3/M4=12、M7=12（最坏 14）、M33/M55=15、M85≈12。
- M7 标称同 M4 但确定性更差（Flash + 缓存抖动），放 ITCM/DTCM 恢复；M0 无可靠 tail-chaining。
- 功耗：µA/MHz 不是电池设备关键指标，**睡眠电流**才是；制程（40nm M33）比内核更省电。
- 实时 / 电池项目这两项应提前到 0.3 后评估。

真实性能以真机测量为准：GPIO 翻转 + 示波器测执行时间，留 ~50% CPU 余量。

---

## 第 1 章：C 语言与 STM32 编程基础

### 1.1 数据定义

一个字节（Byte）等于 8 个比特（bit）。

常用头文件：

- `stdint.h`：定义 `int8_t`、`uint8_t` 等固定位宽类型。
- `stdbool.h`：定义布尔类型。
- `stdio.h`：定义标准输入输出函数。
- `string.h`：字符串相关操作。
- `stdlib.h`：内存管理和通用工具函数。比如：`malloc`、`free`、`rand`。

数据类型：
- `char`：占用一字节的整数类型，不只是用来存字符
- `uint8_t`：8 位无符号整数（0~255）
- `uint16_t`：16 位无符号整数（0~65535）
- `uint32_t`：32 位无符号整数（0~4294967295）

```c
#define TIME_LENGTH 9U
```

- `U`：无符号整型常量后缀（unsigned）。
- `f`：单精度浮点常量后缀（float）；不带后缀默认是 `double`。

### 1.2 结构体与全局变量管理

全局结构体数组按 "头文件声明、源文件定义" 组织。

`.h` 头文件中定义类型并声明外部变量：

```c
typedef struct
{
  uint16_t aim;  // 执行目标
  uint16_t time; // 执行时间
  float cur;     // 当前值
  float inc;     // 增量
} servo_t;

// extern 表示"在其他文件定义"，此处不分配内存
extern servo_t duoji_doing[DJ_NUM];
```

`.c` 文件中进行唯一一次定义：

```c
servo_t duoji_doing[DJ_NUM];
```

浮点类型说明：

- `float`：单精度浮点，4 字节
- `double`：双精度浮点，8 字节
- 示例：`3.0` 默认为 `double`，可写为 `3.0f`、`3.f`、`3e0f` 指定为 `float`

`printf` 宽度补位示例：

- `%2d`：宽度不足时左侧补空格
- `%02d`：宽度不足时左侧补 0

`fabs` 关键字说明：

- floating-point absolute value，计算浮点数的绝对值，返回非负结果。

### 1.3 编程规范与位操作

推荐代码组织顺序：

1. 头文件引用
2. 全局变量与声明
3. 工具函数
4. 缓冲区相关函数
5. 初始化函数
6. 中断函数
7. 发送 / 业务函数

Doxygen 注释建议：

- `@brief`：概括核心功能。
- `@param`：参数名 + 说明，标注输入 / 输出。
- `@retval`：返回值含义，布尔 / 枚举建议列全。

寄存器位操作：

- 置位：`reg |= mask;`
- 清零：`reg &= ~mask;`
- 翻转：`reg ^= mask;`

位掩码通常写作 `(1 << x)`，例如：

- 清第 2 位：`reg &= ~(1 << 2);`
- 取第 4 位：`(reg >> 4) & 0x1`

---

## 第 2 章：开发环境与调试

### 2.1 编译器优化

调试阶段可降低优化等级为 `-O0`，避免变量被优化掉影响排查。

### 2.2 硬件启动配置

STM32 启动前需确认 BOOT 引脚配置与目标启动方式一致（如从 Flash 启动）。

### 2.3 定时器选择注意事项

根据需求选择 16 位或 32 位定时器，重点关注计数范围与分辨率。

---

## 第 3 章：STM32 时钟系统

### 3.1 基础时钟源

- **LSI**：内部低速时钟，常用于看门狗。
- **LSE**：外部低速时钟（常见 32.768 kHz 晶振），常用于 RTC。
- **HSI**：内部高速时钟，无需外部器件，精度一般。
- **HSE**：外部高速时钟，系统主频常用来源。

### 3.2 HSE 配置模式

**Crystal/Ceramic Resonator（无源晶振）：**

- 外接晶体 / 陶瓷谐振器 + 负载电容。
- 适合 USB、UART、定时器等时序敏感场景。

**Bypass Clock Source（有源时钟输入）：**

- 由外部有源时钟源直接输入。
- 不使用片上晶振振荡回路。

### 3.3 时钟分频与倍频

示例：目标系统时钟 100 MHz

- 外部时钟 HSE = 8 MHz。
- 经过 PLL 分频 / 倍频后得到 SYSCLK = 100 MHz。
- APB1 时钟 PCLK1 = HCLK / 2 = 50 MHz。
- 若 APB1 预分频系数大于 1，则 APB1 定时器时钟为 PCLK1 × 2 = 100 MHz。

补充：若要在时钟树启用 LSE，通常需在 RTC 相关配置中开启并选择对应时钟源。

---

## 第 4 章：GPIO 外设与 HAL 库配置

GPIO 输出驱动常见配置项：

- `GPIO Output Level`：引脚初始电平（Low/High）
- `GPIO Mode`：引脚模式

常见输出模式：

- **Output Push-Pull**：推挽输出，可输出高 / 低电平，驱动能力强。
- **Output Open-Drain**：开漏输出，可输出低电平或高阻态；常用于 I2C / 总线共享，需外接上拉电阻。

上下拉配置：

- **Pull-up**：上拉到 VDD（默认高）
- **Pull-down**：下拉到 GND（默认低）
- **No Pull-up/Pull-down**：悬空

输出速度档位（不同系列可能不同，以下以 STM32F1 常见配置为例）：

- Low：2 MHz
- Medium：10 MHz
- High：50 MHz

---

## 第 5 章：中断与实时处理

### 5.1 中断优先级配置

- 周期性控制逻辑（如 Servo_Update）应绑定稳定的定时中断，并合理分配优先级。
- 步进逼近类算法的停止阈值应与步长一致，避免出现跳变。

### 5.2 数据缓冲机制

FIFO（First In First Out，先进先出）常用于串口、ADC、SPI/I2C、USB、DMA 等数据缓冲，降低突发数据导致的丢包风险。

### 5.3 中断共享变量

中断函数与主循环共享的状态变量应定义在全局可见区域，并根据需要加 `volatile` 避免被编译器优化。

---

## 第 6 章：定时与延时

### 6.1 SysTick 定时器

SysTick 是 Cortex-M 内核自带 24 位递减计数器，常用于基础延时与系统节拍。

基本流程：配置时钟源 → 计算重装载值（`SysTick->LOAD`）→ 启动计数 → 等待计数完成。

```c
SysTick->VAL = 0;                          // 清零计数器并清除标志
SysTick->CTRL |= SysTick_CTRL_ENABLE_Msk;  // 启动计数
while (!(SysTick->CTRL & (1 << 16)));      // 等待 COUNTFLAG 置位
SysTick->CTRL &= ~SysTick_CTRL_ENABLE_Msk; // 关闭计数
SysTick->VAL = 0;
```

### 6.2 时间单位换算

- 频率单位 Hz 表示 "每秒周期数"。
- 例如 12.5 MHz 表示每秒 \(12.5 \times 10^6\) 个周期。

---

## 第 7 章：PWM 脉冲宽度调制

### 7.1 硬件 PWM 配置（基于标准库）

STM32 硬件 PWM 常见注意点：

- 仅 `TIM_OCxInit` 不够，通常还需 `TIM_CCxCmd` 使能通道，否则波形可能不输出到引脚。
- 某些场景中，初始化后短暂延时（如 `Delay_ms(100)`）再进入业务配置更稳妥。

### 7.2 PWM 参数计算

- 舵机常用周期 20 ms（50 Hz）。
- 高电平时间常见范围约 500~2500 us（具体以舵机数据手册为准）。
- 万用表读到的常是平均电压：Vavg = Vcc × (Ton / T)。

### 7.3 硬件 PWM 与软件模拟对比

- **硬件 PWM**：占用 CPU 少、稳定性高、适合多通道同步。
- **软件模拟**：依赖中断 / 循环翻转 GPIO，占用高，适合低频和简单场景。

### 7.4 多通道定时器特性

同一定时器的多个通道共享计数器，PWM 周期天然同步，适合联动控制。

---

## 第 8 章：ADC 模数转换

### 8.1 ADC 基础原理

- ADC 基本工作流程
- 常见 ADC 类型（逐次逼近型、Sigma-Delta 型等）
- 关键参数（分辨率、采样率、精度）

### 8.2 基于 STM32CubeMX 的 ADC 快速配置

- 在 CubeMX 中开启 Continuous Conversion Mode（按需）。
- 调用 `HAL_ADCEx_Calibration_Start(...)` 进行校准。
- 调用 `HAL_ADC_Start(...)` 启动转换。
- 调用 `HAL_ADC_GetValue(...)` 读取结果。

### 8.3 ADC 数据获取

- 轮询
- 中断
- DMA
- 多通道采集

### 8.4 ADC 应用实例

- 电压测量
- 温度传感器读取
- 光敏电阻采集
- 电池电量监测

### 8.5 ADC 性能优化

- 采样时间与精度平衡
- 噪声抑制
- 校准策略
- 多通道时序管理

---

## 第 9 章：UART 串口通信

### 9.1 USART 和 UART 的区别

这两个名字很像，功能也很像，初学时最容易混在一起。

- **串口通信**：数据一位一位按顺序发送，不像并口那样同时发很多位。
- **同步通信**：有时钟线，发送端和接收端除了数据线，还会额外使用 "时钟线" 来对齐收发节奏。
- **异步通信**：不额外提供时钟线，双方提前约定好波特率，再按这个速度收发数据。
- **波特率**：串口每秒传输的符号速率，通常可以简单理解为 "通信速度"，常见值有 9600、115200。

#### 9.1.1 UART 是什么

UART 是 Universal Asynchronous Receiver/Transmitter，中文通常叫 "通用异步收发器"。它只支持异步通信。

也就是说，最常见的串口打印、和电脑串口助手通信、模块之间的 TX/RX 通信，很多时候本质上就是在用 UART 方式。

#### 9.1.2 USART 是什么

USART 是 Universal Synchronous/Asynchronous Receiver/Transmitter，中文通常叫 "通用同步 / 异步收发器"。它比 UART 多了一个 "同步" 功能：

- 可以像 UART 一样做异步通信
- 也可以在某些场景下做同步通信

#### 9.1.3 它们的关系怎么理解

可以把 USART 理解成 "功能更全的 UART"。如果一个外设支持 USART，那么它通常也能工作在 UART 常用的异步模式下。

- **UART**：只会异步
- **USART**：既会异步，也会同步

### 9.2 CubeMX 中 USART 的 Mode 说明

在 CubeMX 里配置串口时，常会看到很多 Mode 选项。

#### 9.2.1 Asynchronous

Asynchronous 就是 "异步模式"，这是最常用的模式。它的特点是：

- 一般只用 TX 和 RX 两根信号线
- 不需要额外的时钟线
- 只要双方波特率设置一致，就能通信

这就是我们平时最常用的串口打印、调试输出、连接串口助手、和蓝牙模块或串口模块通信的方式。

#### 9.2.2 Synchronous

Synchronous 就是 "同步模式"。它比异步模式多一根时钟线，发送和接收会参考这根时钟线工作。这样节奏更明确，但硬件连接和配置也更复杂。

#### 9.2.3 Single Wire (Half-Duplex)

Single Wire 表示 "单线半双工"。

- **全双工**：能同时发送和接收
- **半双工**：同一时刻只能发或者收，不能同时进行

这个模式下，收发共用一根数据线，省引脚，但通信方式会受一些限制。

#### 9.2.4 Multiprocessor Communication

这个模式用于多设备通信场景。可以理解成一条总线上挂多个从设备，通过地址区分 "这次数据是发给谁的"。

#### 9.2.5 IrDA

IrDA 是红外通信模式，给红外收发设备使用。

#### 9.2.6 LIN

LIN 是一种汽车电子里常见的低成本串行通信协议，适合一些对速度要求不高、但需要多节点通信的场景。

#### 9.2.7 SmartCard / SmartCard with Card Clock

这是给智能卡一类设备准备的模式。

### 9.3 补充说明

使用 `HAL_UART_Transmit_DMA` 发送大量数据的时候，为了避免下一次发送遇到 busy 的情况，务必启动 DMA 传输完成的回调函数。

---

## 第 10 章：I2C 通信协议

### 10.1 物理层基础

- **总线构成**：包含 SCL（时钟线）与 SDA（双向数据线）两根信号线
- **电气特性**：两根线均通过上拉电阻保持常态高电平，总线为开漏输出结构，通过拉低电平实现通信
- **通信类型**：同步串行通信，SCL 是收发双方的统一时钟基准，所有电平动作都与 SCL 严格对齐

### 10.2 核心时序规则

#### 10.2.1 总线控制信号

- **起始信号（Start，S）**：SCL 为高电平时，SDA 由高电平跳变为低电平，标志一次通信开始
- **停止信号（Stop，P）**：SCL 为高电平时，SDA 由低电平跳变为高电平，标志本次通信结束

#### 10.2.2 数据位传输规则

- **SCL 高电平 = 采样期**：SDA 电平必须保持稳定，接收方在此期间读取 SDA 电平，判定当前位是 0 还是 1
- **SCL 低电平 = 更新期**：SDA 允许翻转电平，发送方在此期间把下一位数据的电平放到 SDA 线上，等待下一个 SCL 高电平采样

### 10.3 字节级传输规则

- **传输单位**：以 1 个字节（8bit）为单次传输的基本单位，数据传输采用高位在前（MSB first）的顺序
- **应答机制**：每传输完 8 位数据后，紧跟 1 位应答位，共占用 9 个 SCL 时钟周期

- 应答位由数据接收方控制 SDA 电平：拉低电平为 ACK（应答成功），保持高电平为 NACK（应答失败 / 结束传输）
- 主机发送数据 / 地址时，由从机返回 ACK；主机读取数据时，由主机返回 ACK，最后一个字节必须返回 NACK 通知从机结束传输

- 标准写操作帧结构（以 24C01 等器件为例）：起始信号 → 从机地址 + 读写位（0 = 写）→ 应答 → 寄存器地址 → 应答 → 数据字节 → 应答 → 停止信号
- **Standard Mode**：最高速率 100 kbit/s
- **快速模式**：最高速率 400 kbit/s

---

## 第 11 章：FreeRTOS 实时操作系统

### 11.1 消息队列

消息队列创建要点（CubeMX + FreeRTOS）：

- 在 CubeMX 中创建消息队列并设置 Queue Name、Queue Size。
- 元素大小通常设置为消息结构体大小（如 `sizeof(msg_t)`）。
- 代码生成后会在 `freertos.c` 中生成对应的 `osMessageQueueId_t` 句柄。

### 11.2 信号量

信号量创建要点：

- CubeMX 的 Timers and Semaphores 分类中选择 Binary Semaphore，设置二进制信号量。
- Semaphore Name 设置变量名，生成代码后会在 `freertos.c` 中生成对应的 `osSemaphoreId_t` 句柄。
- Initial State 设置初始状态，Available 为初始生效，Depleted 为初始失效。

CubeMX 生成的信号量和消息队列句柄在 `freertos.c` 中定义，例如：

```c
usart2DMAHandle = osSemaphoreNew(1, 0, &usart2DMA_attributes);
```

操作信号量的函数为：

- `osSemaphoreAcquire`：获取信号量，填入 timeout 阻塞时间，单位为 tick，`osWaitForever` 表示一直等待直到获取到信号量
- `osSemaphoreRelease`：释放信号量，让等待的 `osSemaphoreAcquire` 函数可以获取到信号量

### 11.3 任务延时

两个 delay 函数的区别：

- `vTaskDelay` 是相对延时，相对于调用它的时刻开始计时，所以如果任务每轮执行时间不固定，那么周期也会漂移。
- `vTaskDelayUntil` 是按绝对唤醒时刻做周期控制，更适合固定周期任务；但它保证的是任务的计划唤醒节拍，不等于在任何情况下都能零抖动地准时开始运行，因为开始运行还会受更高优先级任务和中断影响。

### 11.4 配置注意事项

必须注意 CubeMX 生成的信号量、互斥量、队列、函数入口和实际使用的是不是同一个，否则会出现莫名其妙的死锁，或者无法获取到信号量等问题。常见于修改 CubeMX 后没有对代码进行同步修改。这种情况下编译器不会报错，但是链接器会出错，所以必须额外检查。

---

## 第 12 章：HAL 库函数使用指南

### 12.1 GPIO 操作

`HAL_GPIO_WritePin(GPIOx, GPIO_Pin, GPIO_PinState)`：设置引脚输出电平。

#### 12.1.1 I2C 基础函数

```c
HAL_I2C_Master_Receive(hi2c, devAddr, pData, size, timeout);
HAL_I2C_Master_Transmit(hi2c, devAddr, pData, size, timeout);
```

---

## 第 13 章：实用编程技巧

### 13.1 结构体应用实例

### 13.2 系统信息

- 频率单位换算：1 GHz = 10^9 Hz。

### 13.3 错误排查清单

- 确认 PWM 通道已使能，并确认引脚复用配置正确。
- 初始化后如出现异常，可先尝试短延时再加载业务参数。
- 检查步进控制阈值是否与步长一致，避免波形跳变。
- 检查中断优先级与中断执行时间，避免周期任务抖动。
- 编译优化等级在调试阶段建议使用 `-O0`。
- 检查 BOOT 引脚与启动模式配置是否匹配。
- 检查定时器位宽（16/32 位）是否满足计数范围。
